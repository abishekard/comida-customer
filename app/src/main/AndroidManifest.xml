<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abishek.comida">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cart.CartHome"></activity>
        <activity android:name=".home.HomePage" />
        <activity android:name=".loginAndSignUp.OtpVerification" />
        <activity android:name=".loginAndSignUp.SignUp" />
        <activity android:name=".loginAndSignUp.Login" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splashScreen.onBoarding">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>